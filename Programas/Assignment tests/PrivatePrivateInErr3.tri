
let

  private
    const a ~ 1
    ;
    private
      const b ~ a + 1 ! a visible aquí
    in
      const c ~ b + a
    end
  in
    const d ~ a + c
  end

in

  putint(c) ! c no debe ser visible aquí

end